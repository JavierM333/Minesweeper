#include <map>
#include <SFML/Graphics.hpp>

#ifndef TEMP_LEADERBOARD_H
#define TEMP_LEADERBOARD_H
struct name {
    std::string name;
    int score;
    sf::Text text;
};

class Leaderboard {
    map<int,
};


#endif //TEMP_LEADERBOARD_H
